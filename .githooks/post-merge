#!/bin/sh

echo "[post-merge] Checking dependency files..."

if git diff --name-only ORIG_HEAD HEAD | grep -Eq '(^|/)package\.json$|(^|/)package-lock\.json$'; then
  echo "[post-merge] package files changed. Running npm ci..."
  npm ci
else
  echo "[post-merge] No package changes. Skipping npm ci."
fi
